def dfs(현재상태):
    
    # 1. 가지치기 (불가능하면 종료)
    if 조건위반:
        return
    
    # 2. 종료 조건 (끝까지 갔을 때)
    if 끝까지 도달:
        정답 갱신
        return
    
    # 3. 재귀 호출 (선택지들 탐색)
    dfs(다음상태1)
    dfs(다음상태2)

```
           0
        /     \
      선택    미선택
     /   \     /   \
   ...   ...  ...  ...
```

